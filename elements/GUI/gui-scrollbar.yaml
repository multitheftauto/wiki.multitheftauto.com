name: gui-scrollbar
description: |
  The GUI scrollbar class represents GUI scrollbars in MTA's GUI system. These can be moved with cursor and it's position can be checked and set by script. These are built by default to certain GUI elements ([gridlists](/reference/gui-gridlist)) if they exceed the given size. 

preview_images: 
  - path: "GUI/scrollbar.png"
    description: Example GUI scrollbar.