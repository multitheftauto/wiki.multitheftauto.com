name: gui-font
description: |
  The **GUI font** class represents a custom typeface which can be applied to [GUI widgets](/reference/GuiElement). A GUI font is created with [[guiCreateFont]] and can then used as a parameter in [[guiSetFont]].