# Scraped from: https://wiki.multitheftauto.com/wiki/GetNetworkStats
client:
  name: getNetworkStats
  description: This function returns network status information.
  parameters: []
  examples:
  - path: examples/getNetworkStats-1.lua
    description: This example outputs the local players network status information
      to their console when using the /netstatus command
    side: client
  returns:
    values:
    - type: table
      name: value
    description: 'Returns a table, the indexes in the table are the following:'
  requires_review: true
server:
  name: getNetworkStats
  description: This function returns network status information.
  parameters:
  - name: thePlayer
    type: element
    description: MISSING_PARAM_DESC
    default: nil
  examples: []
  returns:
    values:
    - type: table
      name: value
    description: 'Returns a table, the indexes in the table are the following:'
  requires_review: true
