# Scraped from: https://wiki.multitheftauto.com/wiki/IsVolumetricShadowsEnabled
shared:
  name: isVolumetricShadowsEnabled
  description: This function is used to check whether the shadow enabled or not.
  parameters: []
  examples:
  - path: examples/isVolumetricShadowsEnabled-1.lua
    description: This example used a command to check if volumetric shadows enabled
      or not.
    side: client
  returns:
    values:
    - type: bool
      name: value
    description: Returns true if enabled, false otherwise.
  requires_review: true
