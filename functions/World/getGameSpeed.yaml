# Scraped from: https://wiki.multitheftauto.com/wiki/GetGameSpeed
shared:
  name: getGameSpeed
  description: This function gets the current game speed value.
  parameters: []
  examples:
  - path: examples/getGameSpeed-1.lua
    description: This example adds a 'gamespeed' console command that prints the game
      speed to the chatbox.
    side: server
  returns:
    values:
    - type: float
      name: value
    description: Returns a float representing the speed of the game.
  requires_review: true
