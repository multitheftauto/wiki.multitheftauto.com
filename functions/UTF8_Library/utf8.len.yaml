# Scraped from: https://wiki.multitheftauto.com/wiki/Utf8.len
shared:
  name: utf8.len
  description: Returns the length of the string passed.
  parameters:
  - name: input
    type: string
    description: A string character sequence
  - name: int i
    type: '[,'
    description: MISSING_PARAM_DESC
    default: 1, int j = utf8.len( input
  examples:
  - path: examples/utf8.len-1.lua
    description: This example calculates the length of the input of the command /length
      and shows it in the chatbox.
    side: client
  returns:
    values:
    - type: int utf8.len
      name: value
    description: Returns the length of the string as an integer .
  requires_review: true
