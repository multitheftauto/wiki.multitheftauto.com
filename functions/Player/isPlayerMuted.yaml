# Scraped from: https://wiki.multitheftauto.com/wiki/IsPlayerMuted
shared:
  name: isPlayerMuted
  description: Use this function to check if a player has been muted.
  parameters:
  - name: thePlayer
    type: player
    description: The player you are checking.
  examples:
  - path: examples/isPlayerMuted-1.lua
    description: ''
    side: server
  returns:
    values:
    - type: bool
      name: value
    description: Returns true if the player is muted and false otherwise.
  oop:
    element: player
    method: isMuted
    variable: muted
    static: false
  pair: setPlayerMuted
  requires_review: true
