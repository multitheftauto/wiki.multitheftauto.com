# Scraped from: https://wiki.multitheftauto.com/wiki/GetRandomPlayer
shared:
  name: getRandomPlayer
  description: This function returns a random [player](/wiki/Player "Player").
  parameters: []
  examples:
  - path: examples/getRandomPlayer-1.lua
    description: This code outputs a random player's name.
    side: server
  returns:
    values:
    - type: player
      name: value
    description: Returns a random player , false if the server is empty.
  oop:
    element: Player
    method: )
    static: true
  requires_review: true
