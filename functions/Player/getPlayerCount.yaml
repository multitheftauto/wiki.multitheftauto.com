# Scraped from: https://wiki.multitheftauto.com/wiki/GetPlayerCount
shared:
  name: getPlayerCount
  description: This function returns the number of players currently connected to
    the server.
  parameters: []
  examples:
  - path: examples/getPlayerCount-1.lua
    description: This example displays a chat message with the number of players connected
      to the server when a player joins or quits.
    side: server
  returns:
    values:
    - type: int
      name: value
    description: Returns the number of players connected to the server as an int .
  oop:
    element: Player
    method: )
    static: true
  notes:
  - type: info
    content: '#getElementsByType("player") works the same as this function but also
      works client side unlike this function.'
  meta:
  - needs_checking: 'This function was partially migrated from the old wiki. Please
      review manually:

      - Missing section: Shared implementation of getPlayerCount

      '
  requires_review: true
