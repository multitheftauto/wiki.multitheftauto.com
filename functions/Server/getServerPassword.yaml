# Scraped from: https://wiki.multitheftauto.com/wiki/GetServerPassword
shared:
  name: getServerPassword
  description: This function returns the current password required to join the server.
  parameters: []
  examples:
  - path: examples/getServerPassword-1.lua
    description: This example prints the serverpassword to the player
    side: server
  returns:
    values:
    - type: string
      name: value
    description: Returns the current server password as a string if it has a password,
      if not it returns nil .
  requires_review: true
