# Scraped from: https://wiki.multitheftauto.com/wiki/SetMarkerSize
shared:
  name: setMarkerSize
  description: This function sets the size of the specified marker.
  parameters:
  - name: theMarker
    type: marker
    description: The marker that you wish to set the size of.
  - name: size
    type: float
    description: A float representing new size of the marker.
  examples:
  - path: examples/setMarkerSize-1.lua
    description: This example creates a cylinder marker at the position 0, 0, 2 and
      sets its size to2.5.
    side: server
  - path: examples/setMarkerSize-2.lua
    description: This example creates a cylinder marker at the position 0, 0, 2 and
      plus its size by1by using 'getMarkerSize'.
    side: server
  returns:
    values:
    - type: bool
      name: value
    description: Returns true if successful, false if failed.
  oop:
    element: marker
    method: setSize
    variable: size
    static: false
  pair: getMarkerSize
  meta:
  - needs_checking: 'This function was partially migrated from the old wiki. Please
      review manually:

      - Missing section: Client

      - Missing section: Server

      '
  requires_review: true
