client:
  name: engineGetModelNameFromID
  pair: engineGetModelIDFromName
  description: This function gets the model name of an object model from model ID.
  parameters:
  - name: modelID
    type: int
    description: The model ID of the object.
  examples:
  - path: examples/engineGetModelNameFromID-1.lua
    description: This example gets the model name of an element you target (doesn't work for San Andreas world).
  - path: examples/engineGetModelNameFromID_OOP-1.lua
    oop: true
  returns:
    values:
    - type: string|false
      name: model name
    description: Returns a [[string]] with the name of the object model, **false** otherwise.
  oop:
    element: Engine
    method: getModelNameFromID
    static: true