# Scraped from: https://wiki.multitheftauto.com/wiki/EngineSetPoolCapacity
shared:
  name: engineSetPoolCapacity
  description: This function changes the capacity of the provided pool.
  parameters:
  - name: pool
    type: string
    description: Name of the pool
  - name: capacity
    type: number
    description: New size
  examples: []
  returns:
    values:
    - type: boolean
      name: value
    description: true if the pool capacity was changed and false if not. Throws an
      error if the pool is invalid.
  meta:
  - needs_checking: 'This function was partially migrated from the old wiki. Please
      review manually:

      - Missing section: Engine pools

      '
  requires_review: true
