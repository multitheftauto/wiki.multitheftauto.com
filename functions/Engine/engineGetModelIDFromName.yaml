client:
  name: engineGetModelIDFromName
  pair: engineGetModelNameFromID
  description: This function gets the model ID of an object model from object name.
  parameters:
  - name: modelName
    type: string
    description: The model name of the object.
  examples:
  - path: examples/engineGetModelIDFromName-1.lua
    description: This example gets the model ID and name of an element you target (doesn't work for San Andreas world).
  - path: examples/engineGetModelIDFromName_OOP-1.lua
    oop: true
  returns:
    values:
    - type: int|false
      name: model id
    description: Returns an [[int]] with the ID of the object model, **false** otherwise.
  oop:
    element: Engine
    method: getModelIDFromName
    static: true