client:
  name: engineFreeTXD
  pair: engineRequestTXD
  description: This function is used to un\-assign the specified TXD ID from the [[engineRequestTXD]] assignment.
  parameters:
  - name: txdID
    type: int
    description: The TXD ID you want to have un-assigned.
  examples:
  - path: examples/engineFreeTXD-1.lua
  returns:
    values:
    - type: bool
      name: result
    description: Returns **true** if the TXD was successfully freed, **false** otherwise.
  version:
    added: 1.6.0 r22190