client:
  name: engineStreamingGetModelLoadState
  description: This function returns load state of model.
  parameters:
  - name: modelID
    type: number
    description: ID of the model you want to get flags.
  examples: []
  returns:
    values:
    - type: string
      name: load state
      templateList: model-load-states
    description: Returns string with model load state or throws **error** if invalid model ID is passed.
  version:
    added: 1.6.0 r22676