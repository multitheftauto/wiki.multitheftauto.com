client:
  name: isElementStreamedIn
  description: This function checks whether an element is currently streamed in (not virtualized) and are actual GTA objects in the world. You can force an element to be streamed in using [[setElementStreamable]].
  parameters:
  - name: theElement
    type: element
    description: The element to check whether is streamed in or not.
  examples:
  - path: examples/isElementStreamedIn-1.lua
    description: This command shows you how many objects you have streamed in. Objects can already stream from a very long distance, so even when you can't see some objects, they can still be returned.
  - path: examples/isElementStreamedIn_OOP-1.lua
    oop: true
  returns:
    values:
    - type: bool
      name: result
    description: Returns **true** if the passed element is currently streamed in, **false**
      if it is virtualized.
  oop:
    element: element
    method: isStreamedIn
    variable: streamedIn
    static: false