client:
  name: isElementWaitingForGroundToLoad
  description: This function checks whether MTA has frozen an element because it is
    above map objects which are still loading or not. This can be a [[player]], [[ped]] or [[vehicle]].
  parameters:
  - name: theElement
    type: element
    description: The element to check its frozen waiting for custom map objects to
      load status.
  examples:
  - path: examples/isElementWaitingForGroundToLoad-1.lua
    description: The next code snippet outputs a message when a vehicle respawns far
      away from players, above an object.
  - path: examples/isElementWaitingForGroundToLoad_OOP-1.lua
    description: The next code snippet outputs a message when a vehicle respawns far away from players, above an object.
    oop: true
  returns:
    values:
    - type: bool
      name: result
    description: Returns **true** if the specified element is frozen waiting for collisions
      of custom map objects to load. Returns **false** if it's not or if the specified
      element is invalid.
  oop:
    element: element
    method: isWaitingForGroundToLoad
    variable: waitingForGroundToLoad
    static: false
  notes:
  - type: info
    content: When vehicles are frozen waiting for collisions to load they do not overwrite
      the frozen status set by [[setElementFrozen]].