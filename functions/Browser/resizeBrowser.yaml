# Scraped from: https://wiki.multitheftauto.com/wiki/ResizeBrowser
client:
  name: resizeBrowser
  description: Allows resizing of CEF browsers at runtime.
  parameters: []
  examples:
  - path: examples/resizeBrowser-1.lua
    description: Example for resize browser by command.
    side: client
  notes:
  - type: warning
    content: Do not use this function with onClientRender as it re-creates the underlying
      texture internally (which is an expensive operation).
